#include <sdtdio.h>
#include <stdlib.h>
#include <string.h>
#define max 256

void main(void)
{
	char get_str[max];
	fgets(get_str, max, stdin);




unsigned int string_handle(char *str, char *ins_str , int p1, int p2)
{
	int i=0;
	char new_str[max];
	for(;i<p1;i++)
		new_str[i]=*str[i];
	strcmp







negative_handle

find_pare

void str_div(char str[max], char c)
{
	str
